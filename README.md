# PageFerry CLI

The native Go command-line client for PageFerry.

```sh
go run ./cmd/pageferry --version
go test ./...
```

Application packages belong under `internal/`; the executable entry point stays in
`cmd/pageferry/`.
